<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jpttrindade.br.gdrivetest" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".views.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.CreateNewProjectActivity" />
        <activity android:name=".views.CreateNewRequirementActivity"/>
        <activity
            android:name=".views.RequirementsActivity"
            android:label="@string/title_activity_requirements" >
        </activity>

        <activity android:name=".views.EditRequirementActivity"/>
    </application>

</manifest>
